var animation = lottie.loadAnimation({
    container: document.getElementById('header-animation'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'data.json'
})

var animation = lottie.loadAnimation({
    container: document.getElementById('viewglobal-360'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'data.json'
})